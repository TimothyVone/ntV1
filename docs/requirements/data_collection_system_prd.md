# 加密货币数据采集系统 PRD

## 文档信息
- **项目名称**: 加密货币数据采集系统
- **文档版本**: v1.0
- **创建日期**: 2026-01-18
- **产品经理**: 俞军（AI角色）
- **目标用户**: 个人量化交易者
- **开发周期**: 2周（MVP）

---

## 1. 需求概述

### 1.1 项目背景

用户正在从**长期投资定投**转向**量化交易**，需要建立自己的数据基础设施来支持量化策略。

**现状痛点：**
- 缺乏系统化的历史数据
- 没有数据质量验证机制
- 无法可视化分析市场数据
- 依赖第三方平台，数据不可控

**项目目标：**
建立一套完整的加密货币数据采集、验证、分析系统，为量化交易提供可靠的数据基础。

### 1.2 产品价值

**用户价值公式：**
```
产品价值 = (新体验 - 旧体验) - 替换成本

新体验：
- 自动采集数据，节省时间
- 数据质量验证，提高准确性
- 可视化分析，辅助决策
- 数据自主可控

旧体验：
- 手动从交易所网站下载数据
- 无法验证数据准确性
- 无法自定义分析

替换成本：
- 学习新技术（FastAPI、Vue.js）
- 部署和维护系统
```

**结论：** 对于个人量化交易者，这个系统的价值远大于替换成本。

### 1.3 目标用户

**用户画像：**
- **身份**: 个人投资者，转型量化交易
- **技术背景**: 了解Python，愿意学习新技术
- **交易经验**: 有长期投资经验，开始探索量化策略
- **核心需求**: 获取可靠的市场数据，支持策略开发和回测
- **使用场景**: 本地部署，个人使用

---

## 2. 核心功能需求

### 2.1 功能优先级

**MVP阶段（必须）：**
1. ⭐⭐⭐ 采集K线数据（核心）
2. ⭐⭐⭐ 数据验证（逻辑检查、连续性检查）
3. ⭐⭐⭐ Web界面K线图展示
4. ⭐⭐ 技术指标计算和展示（MA优先）

**第二阶段（迭代）：**
5. ⭐⭐ 数据导出（Excel）
6. ⭐⭐ 交互功能（缩放、拖动）
7. ⭐ 数据统计报告
8. ⭐ 健康检查和监控
9. ⭐ 定时增量更新

**暂不开发：**
- ❌ 跨交易所对比（套利阶段再做）

### 2.2 MVP成功标准（2周后）

**可验证的目标：**
1. ✅ 能够采集BTC/USDT的4小时和1天K线数据（从币安最早数据开始）
2. ✅ 能够在Web页面查看K线图和MA指标（支持交互）
3. ✅ 能够发现数据缺失并生成验证报告

---

## 3. 数据需求

### 3.1 数据类型和周期

**K线数据（MVP阶段）：**

| 优先级 | 时间周期 | 用途 | 保存期限 |
|--------|----------|------|----------|
| 必须 | 4小时 | 趋势判断 | 永久保存 |
| 必须 | 1天 | 大趋势判断 | 永久保存 |
| 第二阶段 | 5分钟 | 日内交易 | 1年 |
| 第二阶段 | 30分钟 | 日内波段 | 永久保存 |
| 第二阶段 | 1小时 | 中期趋势 | 永久保存 |
| 第二阶段 | 1周 | 长期趋势 | 永久保存 |

**实时数据：**
- ✅ 当前价格（WebSocket或轮询）

**暂不需要：**
- ❌ 订单簿数据（套利阶段需要）
- ❌ 1分钟、3分钟K线（数据量大，API压力大）
- ❌ 1月K线（1天足够）

### 3.2 交易对范围

**MVP阶段：**
- BTC/USDT（币安）

**第二阶段：**
- ETH/USDT
- BNB/USDT
- 其他主流交易对

### 3.3 历史数据范围

**采集范围：**
- 币安BTC/USDT：从2017年8月开始（交易所最早数据）
- 预计数据量：约2,500根（1天K线）、15,000根（4小时K线）

**数据更新策略：**
- 初次：全量采集
- 增量：手动触发（后续扩展定时任务）

---

## 4. 数据验证需求

### 4.1 验证类型

**MVP阶段：**

#### 4.1.1 逻辑检查
```python
# 检查规则
最高价 >= max(开盘价, 收盘价, 最低价)
最低价 <= min(开盘价, 收盘价, 最高价)
开盘价 > 0 and 收盘价 > 0
成交量 >= 0
时间戳必须严格递增
时间戳必须是周期的整数倍
```

#### 4.1.2 连续性检查
```python
# 5分钟K线应该连续
expected_interval = 5 * 60  # 300秒
tolerance = 60  # 允许1分钟误差

if (current_timestamp - previous_timestamp) > expected_interval + tolerance:
    标记为数据缺失
```

#### 4.1.3 统计分析
```python
# 单根K线涨跌幅异常标准
4小时K线: 单根涨跌 > 15% 算异常
1天K线: 单根涨跌 > 20% 算异常

# 成交量异常
成交量 > 3倍平均成交量 = 爆量
成交量 < 1/10平均成交量 = 地量
```

**第二阶段：**
- 跨交易所对比（币安 vs 欧易）
- 人工抽查工具

### 4.2 异常数据处理

**MVP阶段：**
- ✅ 标记为"错误"，但保留数据
- ✅ 在图表上用红色空心蜡烛标记
- ✅ 在报告中列出错误数据的索引和时间

**数据质量评分：**
```
评分 = 100 - (逻辑错误×10) - (缺失率×50) - (异常率×30)

≥ 90分: 优秀（绿色）
80-89分: 良好（黄色）
70-79分: 及格（橙色）
< 70分: 需要改进（红色）
```

### 4.3 验证报告

**报告内容：**
```
数据验证报告 - BTC/USDT 4h
================================
验证时间: 2024-01-18 10:30:25
数据范围: 2017-08-01 至 2024-01-18
K线数量: 15,234根

逻辑检查:
  - 错误数据: 0条 (0%)

连续性检查:
  - 缺失数据: 8根 (0.05%)
  - 缺失时间段:
    * 2021-03-15 16:00 - 2021-03-15 20:00 (缺失4根)
    * 2022-06-20 08:00 - 2022-06-20 12:00 (缺失4根)

统计分析:
  - 平均涨跌幅: +0.12%
  - 最大涨幅: +18.3% (2020-03-12 16:00)
  - 最大跌幅: -15.8% (2020-03-13 08:00)
  - 价格波动率: 3.5%
  - 异常K线: 12根 (0.08%)

数据质量评分: 98/100 (优秀)
```

---

## 5. Web界面需求

### 5.1 技术栈

- **前端框架**: Vue.js 3
- **K线图库**: TradingView Lightweight Charts
- **UI组件库**: Element Plus（可选）
- **构建工具**: Vite

### 5.2 界面布局

```
┌─────────────────────────────────────────────────────────┐
│  加密货币数据采集系统                                    │
├─────────────────────────────────────────────────────────┤
│  K线图板块                                               │
│  ┌───────────────────────────────────────────────────┐  │
│  │ [BTC/USDT ▼] [4小时 ▼] [指标设置] [📷导出图片]    │  │
│  │                                                   │  │
│  │  主图：K线 + MA20 + MA50                          │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  TradingView Lightweight Charts             │  │  │
│  │  │  (支持缩放、拖动、点击查看数值)              │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │                                                   │  │
│  │  副图1：成交量（MVP阶段）                         │  │
│  │  副图2：MACD（第二阶段）                          │  │
│  └───────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│  数据管理中心板块                                        │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐      │
│  │数据概览│ │全量采集│ │增量采集│ │数据统计│ │验证报告  │      │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────────┘      │
│                                                          │
│  [📊 导出Excel]  [🔄 健康检查]                           │
└─────────────────────────────────────────────────────────┘
```

### 5.3 技术指标展示

**MVP阶段：**
- ✅ 主图：K线 + MA20 + MA50
- ✅ 副图：成交量
- ✅ 支持勾选切换显示/隐藏

**第二阶段：**
- EMA、MACD、RSI、布林带、ATR、SAR、KDJ

### 5.4 交互功能

**MVP阶段：**
- ✅ 缩放（鼠标滚轮）
- ✅ 拖动（鼠标拖拽）
- ✅ 点击查看具体数值
- ✅ 交易对切换
- ✅ 时间周期切换

**第二阶段：**
- 导出图片
- 自定义时间范围

### 5.5 数据管理中心功能

#### 5.5.1 数据概览
```
数据概览
========
交易对: BTC/USDT
时间周期: 4小时, 1天
数据范围: 2017-08-01 至 2024-01-18
K线数量:
  - 4小时: 15,234根
  - 1天: 2,356根
最后更新: 2024-01-18 10:00:00
数据质量评分: 98/100 (优秀)
```

#### 5.5.2 数据全量采集
```
全量采集配置
============
交易所: [币安 ▼]
交易对: [BTC/USDT ▼]
时间周期: ☑ 4小时 ☑ 1天
采集范围: 从最早数据开始 (2017-08-01)

[开始采集]
进度: ████████████░░░░░░ 60% (9,140/15,234)
预计剩余: 5分钟
```

#### 5.5.3 数据增量采集
```
增量采集配置
============
交易所: [币安 ▼]
交易对: [BTC/USDT ▼]
时间周期: ☑ 4小时 ☑ 1天
采集范围: 从最后更新到现在

[开始采集]
状态: ✓ 采集完成
新增数据: 4小时×2根, 1天×1根
耗时: 3秒
```

#### 5.5.4 数据统计
```
数据统计
========
价格统计:
- 平均涨跌幅: +0.12%
- 最大涨幅: +18.3% (2020-03-12)
- 最大跌幅: -15.8% (2020-03-13)
- 价格波动率: 3.5%

成交量统计:
- 平均成交量: 1,250 BTC
- 爆量次数: 12次
- 地量次数: 8次

数据增长:
- 总数据量: 17,590根
- 月增长: 432根
- 日增长: 14根
```

#### 5.5.5 验证报告
显示数据验证结果（见4.3节）

---

## 6. 数据导出需求

### 6.1 导出图片

**MVP阶段：**
- 导出K线图（当前视图）
- 格式：PNG（72 DPI）
- 文件名：`BTC_USDT_4h_2024-01-18.png`

**第二阶段：**
- 自定义时间范围
- 添加水印
- 导出为SVG（矢量图）

### 6.2 导出Excel

**MVP阶段：**
- 导出原始K线数据
- 导出技术指标数据（MA）
- 导出验证报告
- 文件名：`BTC_USDT_4h_2024-01-18.xlsx`

**Excel结构：**
```
Sheet1: 原始数据
时间戳 | 日期时间 | 开盘 | 最高 | 最低 | 收盘 | 成交量

Sheet2: 技术指标
时间戳 | MA20 | MA50

Sheet3: 验证报告
(见4.3节)
```

---

## 7. 技术架构

### 7.1 整体架构

```
┌─────────────────────────────────────────┐
│         前端 (Vue.js)                    │
│  ┌──────────┐  ┌──────────┐            │
│  │ K线图组件│  │ 数据管理  │            │
│  └──────────┘  └──────────┘            │
└─────────┬───────────────────────────────┘
          │ REST API
┌─────────▼───────────────────────────────┐
│      后端 (FastAPI)                      │
│  ┌──────────┐  ┌──────────┐            │
│  │ API接口  │  │ 业务逻辑  │            │
│  └──────────┘  └──────────┘            │
│  ┌──────────┐  ┌──────────┐            │
│  │ 数据采集  │  │ 数据验证  │            │
│  └──────────┘  └──────────┘            │
└─────────┬───────────────────────────────┘
          │
┌─────────▼───────────────────────────────┐
│      数据层 (SQLite)                     │
│  ┌──────────┐  ┌──────────┐            │
│  │K线数据表 │  │ 配置数据  │            │
│  └──────────┘  └──────────┘            │
└──────────────────────────────────────────┘
          │
┌─────────▼───────────────────────────────┐
│      外部服务                            │
│  ┌──────────┐  ┌──────────┐            │
│  │ 币安API  │  │ 钉钉通知  │            │
│  └──────────┘  └──────────┘            │
└──────────────────────────────────────────┘
```

### 7.2 技术栈

**后端：**
- Python 3.11
- FastAPI（Web框架）
- CCXT（交易所接口）
- SQLAlchemy（ORM）
- Pandas（数据处理）
- NumPy（数值计算）
- Loguru（日志）

**前端：**
- Vue.js 3
- TradingView Lightweight Charts
- Axios（HTTP请求）
- Element Plus（UI组件，可选）

**数据库：**
- SQLite（MVP阶段）
- InfluxDB（第二阶段）

**其他：**
- PyYAML（配置文件）
- Requests（HTTP请求）
- Websocket-client（WebSocket）

### 7.3 项目结构

```
data-collection-system/
├── backend/                    # 后端
│   ├── api/                   # FastAPI接口
│   │   ├── __init__.py
│   │   ├── klines.py         # K线数据API
│   │   ├── indicators.py     # 技术指标API
│   │   ├── validation.py     # 数据验证API
│   │   └── export.py         # 数据导出API
│   ├── collectors/           # 数据采集
│   │   ├── __init__.py
│   │   ├── base.py           # 采集器基类
│   │   └── binance.py        # 币安采集器
│   ├── validators/           # 数据验证
│   │   ├── __init__.py
│   │   ├── logic.py          # 逻辑检查
│   │   ├── continuity.py     # 连续性检查
│   │   └── statistics.py     # 统计分析
│   ├── indicators/           # 技术指标
│   │   ├── __init__.py
│   │   ├── ma.py             # 移动平均线
│   │   └── rsi.py            # RSI指标
│   ├── database/             # 数据库操作
│   │   ├── __init__.py
│   │   ├── models.py         # 数据模型
│   │   └── connection.py     # 数据库连接
│   ├── services/             # 业务服务
│   │   ├── __init__.py
│   │   ├── data_service.py   # 数据服务
│   │   └── notify_service.py # 通知服务
│   ├── utils/                # 工具函数
│   │   ├── __init__.py
│   │   ├── logger.py         # 日志工具
│   │   └── config.py         # 配置工具
│   └── main.py               # FastAPI入口
│
├── frontend/                 # 前端
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── components/       # Vue组件
│   │   │   ├── Chart.vue     # K线图组件
│   │   │   ├── DataManager.vue # 数据管理中心
│   │   │   └── ...
│   │   ├── api/              # API调用
│   │   │   └── index.js
│   │   ├── App.vue
│   │   └── main.js
│   ├── package.json
│   └── vite.config.js
│
├── data/                     # 数据目录
│   ├── database/             # SQLite数据库
│   │   └── trading_data.db
│   └── logs/                 # 日志文件
│       ├── collector.log
│       └── validator.log
│
├── config/                   # 配置文件
│   └── config.yaml           # 主配置文件
│
├── tests/                    # 测试
│   ├── test_collector.py
│   ├── test_validator.py
│   └── test_api.py
│
├── requirements.txt          # Python依赖
├── README.md                 # 项目说明
└── .env.example              # 环境变量示例
```

---

## 8. 数据库设计

### 8.1 表结构

#### 8.1.1 K线数据表

**表名：** `binance_btc_usdt_4h`

```sql
CREATE TABLE binance_btc_usdt_4h (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp INTEGER NOT NULL UNIQUE,  -- Unix时间戳
    datetime TEXT NOT NULL,              -- 日期时间 (YYYY-MM-DD HH:MM:SS)
    open DECIMAL(20, 8) NOT NULL,        -- 开盘价
    high DECIMAL(20, 8) NOT NULL,        -- 最高价
    low DECIMAL(20, 8) NOT NULL,         -- 最低价
    close DECIMAL(20, 8) NOT NULL,       -- 收盘价
    volume DECIMAL(20, 8) NOT NULL,      -- 成交量
    quote_volume DECIMAL(20, 8),         -- 成交额
    trades_count INTEGER,                -- 成交笔数
    data_quality TEXT DEFAULT 'good',    -- 数据质量标记 (good/error/missing)
    error_reason TEXT,                   -- 错误原因
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX idx_timestamp ON binance_btc_usdt_4h(timestamp);
CREATE INDEX idx_datetime ON binance_btc_usdt_4h(datetime);
```

**命名规则：**
- 每个交易对一张表
- 表名格式：`{exchange}_{symbol}_{timeframe}`
- 例如：`binance_btc_usdt_4h`、`binance_eth_usdt_1d`

#### 8.1.2 技术指标缓存表

**表名：** `indicators_cache`

```sql
CREATE TABLE indicators_cache (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    table_name TEXT NOT NULL,             -- 关联的K线表名
    timestamp INTEGER NOT NULL,           -- Unix时间戳
    indicator_name TEXT NOT NULL,         -- 指标名称 (MA20, MA50, RSI, etc.)
    indicator_value DECIMAL(20, 8),       -- 指标值
    calculated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(table_name, timestamp, indicator_name)
);

CREATE INDEX idx_indicators_lookup ON indicators_cache(table_name, timestamp, indicator_name);
```

#### 8.1.3 数据验证日志表

**表名：** `validation_logs`

```sql
CREATE TABLE validation_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    table_name TEXT NOT NULL,             -- 验证的表名
    validation_type TEXT NOT NULL,        -- 验证类型 (logic/continuity/statistics)
    validation_time TIMESTAMP NOT NULL,   -- 验证时间
    total_count INTEGER NOT NULL,         -- 总数据量
    error_count INTEGER DEFAULT 0,        -- 错误数量
    missing_count INTEGER DEFAULT 0,      -- 缺失数量
    quality_score INTEGER,                -- 质量评分 (0-100)
    details TEXT,                         -- 详细信息 (JSON格式)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 8.1.4 采集任务日志表

**表名：** `collection_logs`

```sql
CREATE TABLE collection_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    exchange TEXT NOT NULL,               -- 交易所名称
    symbol TEXT NOT NULL,                 -- 交易对
    timeframe TEXT NOT NULL,              -- 时间周期
    task_type TEXT NOT NULL,              -- 任务类型 (full/incremental)
    start_time TIMESTAMP NOT NULL,        -- 开始时间
    end_time TIMESTAMP,                   -- 结束时间
    status TEXT NOT NULL,                 -- 状态 (running/success/failed)
    total_collected INTEGER DEFAULT 0,    -- 采集总数
    new_count INTEGER DEFAULT 0,          -- 新增数量
    error_count INTEGER DEFAULT 0,        -- 错误数量
    error_message TEXT,                   -- 错误信息
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 8.2 数据采样和归档策略

**MVP阶段：**
- 所有数据永久保存
- 每周手动备份一次

**第二阶段：**
- 5分钟K线：保存1年，然后归档到CSV
- 其他周期：永久保存
- 每日凌晨3点自动备份

---

## 9. API接口设计

### 9.1 K线数据接口

#### 9.1.1 获取K线数据

**接口：** `GET /api/klines/{symbol}/{timeframe}`

**请求参数：**
```json
{
  "symbol": "BTC/USDT",
  "timeframe": "4h",
  "limit": 1000,           // 可选，默认1000
  "start_time": 1705200000, // 可选，起始时间戳
  "end_time": 1705286400    // 可选，结束时间戳
}
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "symbol": "BTC/USDT",
    "timeframe": "4h",
    "total": 1000,
    "klines": [
      {
        "timestamp": 1705200000,
        "datetime": "2024-01-15 00:00:00",
        "open": 42500.50,
        "high": 42800.00,
        "low": 42400.00,
        "close": 42650.25,
        "volume": 1250.5,
        "data_quality": "good"
      },
      // ... 更多K线数据
    ]
  }
}
```

#### 9.1.2 获取最新K线

**接口：** `GET /api/klines/{symbol}/{timeframe}/latest`

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "timestamp": 1705286400,
    "datetime": "2024-01-15 04:00:00",
    "open": 42650.25,
    "high": 42900.00,
    "low": 42550.00,
    "close": 42800.50,
    "volume": 1180.3
  }
}
```

### 9.2 技术指标接口

#### 9.2.1 获取移动平均线

**接口：** `GET /api/indicators/{symbol}/{timeframe}/ma`

**请求参数：**
```json
{
  "periods": [20, 50]       // MA周期
}
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "MA20": [
      {
        "timestamp": 1705200000,
        "value": 42300.50
      },
      // ...
    ],
    "MA50": [
      {
        "timestamp": 1705200000,
        "value": 41800.25
      },
      // ...
    ]
  }
}
```

### 9.3 数据采集接口

#### 9.3.1 开始全量采集

**接口：** `POST /api/collect/full`

**请求参数：**
```json
{
  "exchange": "binance",
  "symbol": "BTC/USDT",
  "timeframes": ["4h", "1d"]
}
```

**响应：**
```json
{
  "code": 200,
  "message": "Collection task started",
  "data": {
    "task_id": "task_123456",
    "status": "running",
    "estimated_time": 300  // 秒
  }
}
```

#### 9.3.2 查询采集进度

**接口：** `GET /api/collect/status/{task_id}`

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "task_id": "task_123456",
    "status": "running",
    "progress": 60.5,
    "collected": 9140,
    "total": 15112,
    "estimated_remaining": 120
  }
}
```

### 9.4 数据验证接口

#### 9.4.1 触发数据验证

**接口：** `POST /api/validate`

**请求参数：**
```json
{
  "table_name": "binance_btc_usdt_4h",
  "validation_types": ["logic", "continuity", "statistics"]
}
```

**响应：**
```json
{
  "code": 200,
  "message": "Validation completed",
  "data": {
    "validation_id": "val_123456",
    "quality_score": 98,
    "errors": {
      "logic": 0,
      "continuity": 8,
      "statistics": 12
    }
  }
}
```

#### 9.4.2 获取验证报告

**接口：** `GET /api/validate/report/{table_name}`

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "table_name": "binance_btc_usdt_4h",
    "validation_time": "2024-01-18 10:30:25",
    "total_count": 15234,
    "quality_score": 98,
    "errors": [
      {
        "type": "continuity",
        "count": 8,
        "details": "2021-03-15 16:00 - 2021-03-15 20:00 (缺失4根)"
      }
    ]
  }
}
```

### 9.5 数据导出接口

#### 9.5.1 导出Excel

**接口：** `POST /api/export/excel`

**请求参数：**
```json
{
  "symbol": "BTC/USDT",
  "timeframe": "4h",
  "start_date": "2023-01-01",
  "end_date": "2024-01-18",
  "include": ["raw_data", "indicators", "validation_report"]
}
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "download_url": "/downloads/BTC_USDT_4h_2024-01-18.xlsx",
    "file_size": 2048576
  }
}
```

### 9.6 系统状态接口

#### 9.6.1 获取数据概览

**接口：** `GET /api/system/overview`

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "symbols": [
      {
        "symbol": "BTC/USDT",
        "timeframes": [
          {
            "timeframe": "4h",
            "count": 15234,
            "date_range": "2017-08-01 至 2024-01-18",
            "last_update": "2024-01-18 10:00:00",
            "quality_score": 98
          },
          {
            "timeframe": "1d",
            "count": 2356,
            "date_range": "2017-08-01 至 2024-01-18",
            "last_update": "2024-01-18 10:00:00",
            "quality_score": 99
          }
        ]
      }
    ],
    "total_size": "15.2 MB",
    "last_backup": "2024-01-17 04:00:00"
  }
}
```

---

## 10. 数据采集方案

### 10.1 采集流程

```
┌─────────────┐
│ 1. 初始化    │
└──────┬──────┘
       │
┌──────▼──────┐
│ 2. 创建表    │
└──────┬──────┘
       │
┌──────▼──────┐
│ 3. 获取最早  │
│    时间戳    │
└──────┬──────┘
       │
┌──────▼──────┐
│ 4. 分批采集  │
│  (1000根/批)│
└──────┬──────┘
       │
   ┌───┴───┐
   │       │
┌───▼───┐ ┌─▼────────┐
│成功？ │ │否        │
└───┬───┘ └─┬────────┘
    │是     │重试3次
┌───▼───┐   │
│5. 存储│   │
└───┬───┘   │
    │   ┌───▼──┐
    │   │失败？│
    │   └───┬──┘
    │       │记录日志
    │   ┌───▼──┐
    └───►6. 继续│
        └───┬──┘
            │
        ┌───▼──┐
        │7. 完成│
        │生成报告│
        └──────┘
```

### 10.2 API限流处理

```python
# 请求队列控制
class RateLimiter:
    def __init__(self, max_requests_per_minute=1200):
        self.max_requests = max_requests_per_minute
        self.requests = []
        self.lock = asyncio.Lock()

    async def acquire(self):
        async with self.lock:
            now = time.time()
            # 移除1分钟前的请求记录
            self.requests = [r for r in self.requests if now - r < 60]

            if len(self.requests) >= self.max_requests:
                # 等待直到可以发送请求
                sleep_time = 60 - (now - self.requests[0])
                await asyncio.sleep(sleep_time)
                self.requests.pop(0)

            self.requests.append(now)

# 使用示例
limiter = RateLimiter(max_requests_per_minute=1000)  # 保守值

async def fetch_klines(symbol, timeframe, limit):
    await limiter.acquire()
    # 发送API请求
    data = await exchange.fetch_ohlcv(symbol, timeframe, limit=limit)
    return data
```

### 10.3 错误处理

```python
class Collector:
    MAX_RETRIES = 3
    RETRY_DELAY = 5  # 秒

    async def collect_with_retry(self, symbol, timeframe, since, limit):
        for attempt in range(self.MAX_RETRIES):
            try:
                data = await self.fetch_klines(symbol, timeframe, since, limit)
                return data, None

            except NetworkError as e:
                if attempt < self.MAX_RETRIES - 1:
                    logger.warning(f"网络错误，第{attempt + 1}次重试...")
                    await asyncio.sleep(self.RETRY_DELAY)
                else:
                    logger.error(f"采集失败，已重试{self.MAX_RETRIES}次")
                    return None, str(e)

            except ExchangeError as e:
                logger.error(f"交易所错误: {e}")
                return None, str(e)
```

### 10.4 进度跟踪

```python
class CollectionTask:
    def __init__(self, task_id, symbol, timeframe):
        self.task_id = task_id
        self.symbol = symbol
        self.timeframe = timeframe
        self.total = 0
        self.collected = 0
        self.errors = 0
        self.status = "running"
        self.start_time = time.time()

    def update_progress(self, collected):
        self.collected = collected
        self.status = "running"

    def get_progress(self):
        if self.total == 0:
            return 0
        return (self.collected / self.total) * 100

    def to_dict(self):
        return {
            "task_id": self.task_id,
            "symbol": self.symbol,
            "timeframe": self.timeframe,
            "status": self.status,
            "progress": self.get_progress(),
            "collected": self.collected,
            "total": self.total,
            "errors": self.errors,
            "elapsed_time": time.time() - self.start_time
        }
```

---

## 11. 开发计划

### 11.1 第一周（1月18日-1月24日）

#### Day 1: 环境搭建和项目初始化
- [ ] 创建项目目录结构
- [ ] 初始化Python虚拟环境
- [ ] 安装依赖（FastAPI、CCXT、SQLAlchemy等）
- [ ] 初始化Vue.js项目
- [ ] 配置开发环境（VS Code、ESLint等）
- [ ] 创建SQLite数据库和表结构

#### Day 2-3: 数据采集器开发
- [ ] 实现采集器基类
- [ ] 实现币安采集器
- [ ] 实现请求队列和限流控制
- [ ] 实现错误处理和重试机制
- [ ] 实现进度跟踪
- [ ] 编写采集器单元测试

#### Day 4: 数据验证开发
- [ ] 实现逻辑检查
- [ ] 实现连续性检查
- [ ] 实现统计分析
- [ ] 实现验证报告生成
- [ ] 编写验证器单元测试

#### Day 5: 后端API开发
- [ ] 实现K线数据API
- [ ] 实现数据采集API
- [ ] 实现数据验证API
- [ ] 实现系统状态API
- [ ] 编写API测试

### 11.2 第二周（1月25日-1月31日）

#### Day 6: 技术指标计算
- [ ] 实现MA指标计算
- [ ] 实现指标缓存机制
- [ ] 实现指标API
- [ ] 编写指标单元测试

#### Day 7-8: 前端开发
- [ ] 实现K线图组件（TradingView）
- [ ] 实现数据管理面板
- [ ] 实现交易对和周期切换
- [ ] 实现MA指标叠加
- [ ] 实现交互功能（缩放、拖动）

#### Day 9: 数据导出功能
- [ ] 实现图片导出
- [ ] 实现Excel导出
- [ ] 前端导出按钮集成

#### Day 10: 集成测试
- [ ] 端到端测试（采集→验证→展示）
- [ ] 压力测试（大数据量）
- [ ] 错误场景测试
- [ ] 性能优化

#### Day 11-12: 文档和优化
- [ ] 编写README
- [ ] 编写API文档
- [ ] 编写用户手册
- [ ] 代码优化和重构

#### Day 13-14: 部署和验收
- [ ] 本地部署测试
- [ ] 功能验收（MVP目标）
- [ ] 问题修复
- [ ] 准备演示

### 11.3 里程碑

| 日期 | 里程碑 | 验收标准 |
|------|--------|----------|
| Day 5 | 数据采集完成 | 能成功采集BTC 4小时K线数据 |
| Day 6 | 数据验证完成 | 能生成验证报告 |
| Day 8 | 后端API完成 | API可通过Postman测试 |
| Day 10 | 前端完成 | 能在浏览器查看K线图 |
| Day 12 | 导出功能完成 | 能导出Excel和图片 |
| Day 14 | MVP完成 | 通过3项验收标准 |

---

## 12. 验收标准

### 12.1 MVP验收标准（2周后）

**标准1：数据采集**
- [ ] 能成功采集BTC/USDT的4小时K线数据（从2017年开始）
- [ ] 能成功采集BTC/USDT的1天K线数据
- [ ] 数据正确存储到SQLite数据库
- [ ] 采集过程中有进度显示
- [ ] 采集失败有错误日志

**标准2：Web界面展示**
- [ ] 能在浏览器查看K线图（4小时、1天）
- [ ] K线图支持MA20和MA50指标
- [ ] 支持缩放和拖动
- [ ] 支持点击查看具体数值
- [ ] 支持切换交易对和时间周期

**标准3：数据验证**
- [ ] 能检测数据缺失
- [ ] 能生成验证报告
- [ ] 报告包含质量评分
- [ ] 报告列出缺失的时间段

### 12.2 性能验收标准

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 采集速度 | > 100根/秒 | 采集1000根K线< 10秒 |
| API响应时间 | < 500ms | 调用API获取1000根K线 |
| 前端渲染时间 | < 2秒 | 加载1000根K线到图表 |
| 数据库查询 | < 100ms | 查询1000根K线 |
| 内存占用 | < 500MB | 运行系统时监控 |

### 12.3 质量验收标准

**代码质量：**
- [ ] 代码符合PEP 8规范
- [ ] 关键函数有文档注释
- [ ] 单元测试覆盖率 > 60%
- [ ] 无Critical级别的代码问题

**数据质量：**
- [ ] 采集的数据准确率 > 99%
- [ ] 数据缺失率 < 1%
- [ ] 数据验证能发现问题

**用户体验：**
- [ ] 界面友好、操作简单
- [ ] 错误提示清晰明确
- [ ] 加载状态有反馈

---

## 13. 风险评估

### 13.1 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| API限流 | 高 | 中 | 实现请求队列，降低采集速度 |
| 网络不稳定 | 中 | 高 | 多重重试机制，断点续传 |
| SQLite性能 | 中 | 低 | 添加索引，后续切换到InfluxDB |
| 前端学习曲线 | 中 | 中 | 使用成熟的图表库，参考示例代码 |

### 13.2 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 数据质量问题 | 高 | 低 | 完善的数据验证机制 |
| 交易所API变更 | 中 | 低 | 使用CCXT统一接口 |
| 系统稳定性 | 中 | 中 | 完善的错误处理和日志 |

### 13.3 进度风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 开发时间延期 | 中 | 中 | 分阶段交付，优先核心功能 |
| 技术学习时间 | 低 | 中 | 提前学习，边做边学 |

---

## 14. 后续规划

### 14.1 第二阶段（1个月后）

**功能扩展：**
- [ ] 支持更多时间周期（5分钟、30分钟、1小时）
- [ ] 支持更多交易对（ETH、BNB等）
- [ ] 支持更多技术指标（MACD、RSI、布林带等）
- [ ] 定时增量更新（crontab）
- [ ] 实时数据采集（WebSocket）

**性能优化：**
- [ ] 切换到InfluxDB数据库
- [ ] 实现数据归档策略
- [ ] 添加Redis缓存

### 14.2 第三阶段（3个月后）

**高级功能：**
- [ ] 跨交易所对比（币安 vs 欧易）
- [ ] 套利机会识别
- [ ] 策略回测功能
- [ ] 移动端适配

**运维增强：**
- [ ] 云端部署
- [ ] 监控告警
- [ ] 自动备份

---

## 15. 总结

### 15.1 核心价值

这个数据采集系统将为你的量化交易提供：

1. **可靠的数据基础**
   - 自动采集历史数据
   - 完善的数据验证
   - 数据质量评分

2. **强大的分析能力**
   - 专业K线图展示
   - 多种技术指标
   - 数据导出功能

3. **灵活的扩展性**
   - 模块化设计
   - 易于添加新交易所
   - 易于添加新指标

### 15.2 成功关键

**MVP阶段：**
- ✅ 聚焦核心功能（采集、验证、展示）
- ✅ 快速迭代（2周交付）
- ✅ 用户反馈驱动

**长期发展：**
- ✅ 持续优化性能
- ✅ 逐步添加功能
- ✅ 建立数据优势

### 15.3 下一步行动

1. **确认需求** - 仔细阅读PRD，确认无遗漏
2. **准备环境** - 安装Python 3.11、Node.js、VS Code
3. **开始开发** - 按照11.1开发计划执行
4. **持续沟通** - 遇到问题及时反馈

---

**让我们开始构建这个系统吧！** 🚀

---

**文档结束**

如有任何疑问或需要调整，请随时提出。
